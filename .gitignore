node_modules/
.next/
dist/
build/
coverage/
*.log
*.tsbuildinfo
next-env.d.ts

# per-worktree local env — never shared between agent teams.
# Ignore EVERY .env variant, then re-allow the checked-in template.
# (The previous three-pattern list missed .env.production, .env.development, etc.)
.env*
!.env.example

# supabase local artifacts
supabase/.branches/
supabase/.temp/

# test artifacts
playwright-report/
test-results/
.playwright/

.DS_Store
.vscode/
.idea/
*.timestamp-*.mjs
