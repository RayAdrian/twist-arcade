import type { PlayerId } from "@twist-arcade/engine";
import type { ResolvedRulesetConfig } from "./engine-internal";
export declare function fadeoutHeuristic(queues: readonly [readonly number[], readonly number[]], player: PlayerId, config: ResolvedRulesetConfig): number;
