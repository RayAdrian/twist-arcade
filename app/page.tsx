// Placeholder home route — the shell team owns the real Library home (roadmap Phase 1).
// This page only proves the workspace bootstrap wires up: Next 15 App Router + the
// workspace packages resolve.
export default function HomePage() {
  return (
    <main>
      <h1>Twist Arcade</h1>
      <p>Platform spine under construction.</p>
    </main>
  );
}
