// games/__GAME_ID__/manifest.ts — GameManifest (plan §5.2), SOLO SCORE-CHASE format. Data
// only.
//
// TODO(you): title, classic, ruleSentence, tags, estMinutes are all placeholders — fill in the
// real ones before this game ships. `assertRuleSentenceLength` below is a hard <=90-char
// constraint (also asserted as a real test in manifest.test.ts — G-12).

import type { GameManifest } from "@twist-arcade/game-spec";
import { assertRuleSentenceLength } from "@twist-arcade/game-spec";

export const RULE_SENTENCE = "TODO: describe the twist in one sentence, <=90 characters.";

export const manifest: GameManifest = {
  id: "__GAME_ID__",
  title: "__TITLE__", // TODO(you)
  classic: "TODO", // TODO(you): the classic game this twists, e.g. "Minesweeper"
  ruleSentence: RULE_SENTENCE,
  tags: [], // TODO(you): add "misere" iff losing-fast risk needs the Suicide probe
  estMinutes: 2, // TODO(you)
  modes: { bot: false, hotseat: false, asyncLink: false }, // solo games: no opponent modes
  players: { min: 1, max: 1 },
  difficultyTiers: [], // solo games have no bot-tier ladder (there is no opponent)

  solo: {
    format: "score-chase",
    moveCap: 2000, // default; structural termination is still mandatory (this is a tripwire)
    scoreMonotone: true, // TODO(you): confirm — the placeholder score() never decreases
  },
};

assertRuleSentenceLength(manifest);
